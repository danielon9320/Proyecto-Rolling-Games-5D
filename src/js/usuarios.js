export default class Usuarios{
        constructor(userName, email, password, estado){
                this.userName = userName;
                this.email = email;
                this.password = password;
                this.estado = estado;
            
        }
}